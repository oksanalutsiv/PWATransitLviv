---
title: Foundations/Spacing & Radius
---

# Spacing & Radius

Defined in `src/tokens/spacing.css`.

## Space Scale

export const spaces = [
  { token: '--space-half', value: '2px' },
  { token: '--space-1',    value: '4px' },
  { token: '--space-2',    value: '8px' },
  { token: '--space-3',    value: '12px' },
  { token: '--space-4',    value: '16px' },
  { token: '--space-5',    value: '20px' },
  { token: '--space-6',    value: '24px' },
  { token: '--space-8',    value: '32px' },
  { token: '--space-10',   value: '40px' },
  { token: '--space-12',   value: '48px' },
]

<div style={{ display: 'flex', flexDirection: 'column', gap: 8, padding: '8px 0' }}>
  {spaces.map(s => (
    <div key={s.token} style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
      <div style={{ width: s.value, height: 24, background: '#9C1A87', borderRadius: 3, flexShrink: 0 }} />
      <code style={{ fontSize: 12, color: '#3F3F3F', minWidth: 140 }}>{s.token}</code>
      <span style={{ fontSize: 12, color: '#ADADAD' }}>{s.value}</span>
    </div>
  ))}
</div>

## Border Radius

<div style={{ display: 'flex', gap: 24, padding: '8px 0', flexWrap: 'wrap', alignItems: 'flex-end' }}>
  {[
    { token: '--radius-sm',   value: '6px',    size: 40 },
    { token: '--radius-md',   value: '12px',   size: 60 },
    { token: '--radius-lg',   value: '18px',   size: 80 },
    { token: '--radius-full', value: '9999px', size: 48 },
  ].map(r => (
    <div key={r.token} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 8 }}>
      <div style={{ width: r.size, height: r.size, background: 'rgba(63,63,63,0.12)', border: '2px solid #3F3F3F', borderRadius: r.value }} />
      <code style={{ fontSize: 11, color: '#3F3F3F' }}>{r.token}</code>
      <span style={{ fontSize: 11, color: '#ADADAD' }}>{r.value}</span>
    </div>
  ))}
</div>

## Shadows

<div style={{ display: 'flex', gap: 32, padding: '16px 0', flexWrap: 'wrap' }}>
  {[
    { token: '--shadow-sm', value: '0 1px 4px rgba(0,0,0,0.06)',  label: 'SM' },
    { token: '--shadow-md', value: '0 2px 12px rgba(0,0,0,0.12)', label: 'MD' },
    { token: '--shadow-lg', value: '0 4px 24px rgba(0,0,0,0.12)', label: 'LG' },
  ].map(s => (
    <div key={s.token} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12 }}>
      <div style={{ width: 80, height: 80, background: '#fff', borderRadius: 12, boxShadow: s.value }} />
      <code style={{ fontSize: 11, color: '#3F3F3F' }}>{s.token}</code>
    </div>
  ))}
</div>
